# 学生生活一站式社区 AI 助手

## 需求分析说明书（敏捷需求基线）

文档版本：V1.0（已确认基线）  
编制日期：2026-07-12  
适用阶段：需求分析 / MVP 范围确认  
项目团队：4 人敏捷开发小组

> 本文档是后续概要设计、详细设计、OpenAPI 接口契约、数据库设计、测试用例和 Vibe Coding 提示词的唯一需求基线。未标记为 MVP 的功能进入产品待办池，不作为首期验收阻断项。

## 1. 文档目的与依据

### 1.1 目的

本文档用于统一产品范围、用户角色、业务规则、功能需求、非功能需求、验收标准、四人分工和前后端联调边界，避免开发过程中出现“各自理解、接口后补、模块相互改库”的问题。

### 1.2 参考资料

1. 《06. 学生生活一站式社区 AI 助手》：业务愿景、12 类业务能力及完整功能清单。
2. 《10. 项目总体要求》：知识库、RAG 对话、Agent、ETL、安全与非功能要求。
3. 《11. 文档提交》：PRD、原型、Sprint 报告、数据库、API、测试和验收交付要求。
4. FastAPI 官方资料：基于 OpenAPI/JSON Schema 自动生成交互式接口文档，并支持类型校验、认证、SSE、WebSocket 与测试客户端。
5. OpenAPI Initiative：OpenAPI 是与语言无关的 HTTP API 描述标准，可支持理解接口、生成客户端和自动化测试。
6. Vue、Vite、SQLAlchemy、PostgreSQL 官方资料：用于技术可行性核验，不替代业务需求。

### 1.3 原始需求解读原则

- 《06》给出的是长期产品全景，不等同于四人团队首期必须全部实现。
- 《10》明确说明功能按难度尽量完成、“不要求全部做完”，因此采用 MVP + 增量待办池管理。
- 技术适配示例（如 Chroma、bge-small-zh-v1.5）视为首选方案，不视为不可替换的业务约束；替换时必须保持接口和验收行为不变。
- 涉及教务、后勤、宿管、财务、心理咨询等校内系统的真实对接，在没有接口授权前一律使用适配器 + Mock 数据，不伪造“已对接”。
- 心理危机识别仅提供风险提示与人工升级，不进行医学诊断，不把生成式 AI 作为紧急救援主体。

## 2. 产品定义

### 2.1 产品定位

系统是面向在校学生、学校职能部门和平台运营人员的校园生活统一入口。学生通过自然语言或场景化入口获取校园政策与生活信息、办理事项、提交服务请求、参与校园社区；工作人员通过知识维护、内容治理、服务处理和数据看板持续改善校园服务。

### 2.2 核心问题

1. 校园政策、通知、办事指南和生活信息分散，学生“不知道去哪里找”。
2. 复杂事务缺少清晰材料、步骤、联系人和进度说明，学生“知道要办但不会办”。
3. 校园社区、朋辈经验、二手与失物信息割裂，资源匹配效率低。
4. 学校缺乏对高频诉求、知识盲区、服务质量和内容风险的统一观察窗口。

### 2.3 产品目标

- G1：提供有来源、可追溯、会兜底的校园知识问答。
- G2：把常用生活服务和事项办理收敛到统一入口。
- G3：形成可信、可治理的校园互动与互助空间。
- G4：形成知识维护、用户反馈、效果评估、持续优化闭环。
- G5：通过契约优先和模块化边界，使 4 名开发者能够并行开发、独立测试、稳定联调。

### 2.4 首期成功指标

| 指标 | MVP 目标 | 统计口径 |
|---|---:|---|
| 标准问题命中率 | ≥ 80% | 冻结测试集内能返回相关答案且引用正确的问题占比 |
| 引用可用率 | 100% | 非兜底 RAG 答案至少含 1 个可打开的来源 |
| 高优先级流程完成率 | ≥ 90% | 报修、办事指南、失物发布等核心流程成功提交占比 |
| API 契约通过率 | 100% | OpenAPI 校验和前端契约测试中的 P0 接口 |
| 核心代码测试覆盖率 | ≥ 70% | 后端核心 service/domain 层行覆盖率 |
| 非 LLM API P95 | < 500 ms | 本地验收数据量、排除文件上传和异步任务 |
| 用户可感知首包时间 | < 2 s | Chat SSE 首个 `delta` 事件，Mock/可用模型环境下 |

## 3. 用户角色与权限边界

| 角色 | 核心诉求 | 允许的主要操作 |
|---|---|---|
| 普通学生 | 咨询、办事、报修、互动、交易、失物、反馈 | 访问本人数据；发布与管理本人内容；不能查看他人隐私数据 |
| 学生干部/内容协作者 | 维护活动、专题与经验内容 | 在授权分区内创建和编辑内容，不能执行平台级配置 |
| 辅导员/服务处理员 | 处理学生服务、查看授权范围内风险提示 | 仅查看职责范围内工单与必要信息，禁止浏览无关心理倾诉原文 |
| 部门知识管理员 | 维护本部门知识资产 | 管理被授权知识库、文档、版本和发布状态 |
| 社区运营员 | 内容审核与社区运营 | 审核、下架、置顶、封禁；匿名内容默认不可反查真实身份 |
| 平台管理员 | 账号、角色、模型、系统与审计管理 | 平台级配置；高风险操作二次确认并写入审计日志 |
| 审计/观察角色 | 查看合规与运行记录 | 只读访问脱敏日志和统计，不得修改业务数据 |

## 4. 范围管理

### 4.1 MVP 范围（首期必须完成）

MVP 按 4 个低耦合业务域划分，每个域包含可独立演示的前端页面、后端路由、数据表和自动化测试。

| 模块 | MVP 业务能力 | 独立验收输出 |
|---|---|---|
| M1 AI 知识与对话 | 知识库/文档上传、解析、切分、索引；单/多轮 RAG 问答；引用、兜底、SSE；反馈 | 可上传校园文档并在聊天页得到带引用答案 |
| M2 校园服务中心 | 办事指南；材料清单；部门联系人；宿舍报修工单；本人进度查询 | 可浏览指南、提交报修、跟踪状态；外部系统使用 Mock 适配器 |
| M3 校园社区与互助 | 话题/帖子/评论/点赞；活动发布与报名；失物/拾物发布、筛选、候选匹配 | 社区和失物流程可独立运行，内容进入审核状态机 |
| M4 平台治理与基础能力 | 登录、JWT、RBAC；用户/角色；敏感词与内容审核；配置；审计；基础数据看板 | 所有模块统一认证授权，可追踪关键操作与基础指标 |

### 4.2 增量范围（MVP 后按优先级实现）

- P1：智能填单、真实办事进度适配器、水电查询、二手交易、积分、个性化推荐、知识版本回滚、混合检索、对话质量评估。
- P2：学习搭子/一对一朋辈咨询、租借/拼单、志愿时长、推理诊断、自动回归测试、模型路由、多模态输入。
- P3/研究性：真实支付、跨部门自动流转、模型微调、复杂 Pandas Agent、心理危机联动、全量学工态势感知。

### 4.3 明确不在首期范围

- 不接管学校教务、财务、门禁、心理咨询等系统的真实生产写操作。
- 不保存银行卡、支付密码等支付敏感信息，不自建资金托管。
- 不做心理疾病诊断、治疗建议或完全自动化危机处置。
- 不开放 LLM 生成代码在主机直接执行；Pandas 分析若实现，必须置于受限沙箱。
- 不以“展示全部长期功能菜单”作为完成标准；未实现入口不得造成可点击但无结果的假功能。

## 5. 总体业务流程

### 5.1 RAG 咨询主流程

1. 学生创建或继续会话并提交问题。
2. 系统完成认证、限流、输入安全检查和意图识别。
3. 系统结合会话上下文重写查询，并在用户有权访问的知识库中检索。
4. 结果低于阈值时进入兜底；达到阈值时生成回答并保留引用映射。
5. 后端通过 SSE 依次发送元数据、文本增量、引用、完成或错误事件。
6. 系统持久化问题、答案、模型、耗时、检索结果摘要和请求 ID。
7. 学生可点赞、点踩或提交修正建议，反馈关联到会话与消息。

### 5.2 文档入库主流程

1. 知识管理员创建知识库并选择默认 Embedding 与切分规则。
2. 上传 txt/docx/pdf/md 文件；系统校验格式、大小、哈希和访问权限。
3. 文件进入异步任务：解析 → 清洗 → 切分 → 向量化 → 建立索引。
4. 每一步写入状态和错误原因；失败任务可重试，不产生半完成的可检索版本。
5. 管理员预览 Chunk、元数据与来源位置后发布文档。
6. 只有已发布且未过期文档进入学生端检索范围。

### 5.3 校园服务主流程

1. 学生从场景入口或 AI 推荐进入办事指南/报修。
2. 系统根据校区、学生类型和事项类型返回步骤、材料、地点、时间与联系人。
3. 报修场景通过结构化表单收集宿舍、故障类型、描述和可维修时段。
4. 工单提交采用幂等键防止重复创建，并进入状态机。
5. 处理员更新状态；学生只能查询本人或本人宿舍授权的工单。

### 5.4 社区和失物主流程

1. 用户提交帖子、评论、活动或失物信息。
2. 内容经过格式校验、敏感词检查和风险分级。
3. 低风险内容按策略发布，高风险内容进入人工审核。
4. 失物记录以类别、地点、时间和文本/图片特征生成候选匹配，仅通知候选双方，不公开私人联系方式。
5. 用户可举报，运营员按审核状态机处置，所有处置写审计日志。

## 6. 功能需求与验收标准

### 6.1 M1 AI 知识与对话

| ID | 优先级 | 需求 | 可验收标准 |
|---|---|---|---|
| KB-001 | P0 | 创建、查询、编辑、逻辑删除知识库；按知识库隔离数据 | 名称唯一；删除前检查引用；无权限访问返回 403 |
| KB-002 | P0 | 批量上传 txt/docx/pdf/md，显示处理进度 | 非法格式返回 415；重复文件可识别；任务状态可轮询 |
| KB-003 | P0 | 解析、清洗、递归切分并生成向量 | Chunk 包含 `chunk_id/document_id/source_location/page_number` 等元数据 |
| KB-004 | P0 | 文档列表、预览 Chunk、发布/停用、删除 | 停用后新会话不可检索；删除同步清理索引 |
| CHAT-001 | P0 | 单轮知识问答 | 标准问题返回答案、消息 ID 和至少一个引用，否则明确兜底 |
| CHAT-002 | P0 | 多轮上下文 | 在配置轮数内能解析“这个/上一条”等指代；支持清空会话 |
| CHAT-003 | P0 | 流式响应 | 按统一 SSE 事件协议发送；断流可识别，最终状态可查询 |
| CHAT-004 | P0 | 引用和原文跳转 | 引用包含文档、Chunk、位置与可访问预览地址；越权来源不返回 |
| CHAT-005 | P0 | 低置信兜底 | 低于阈值时不编造，返回建议部门/可选下一步 |
| CHAT-006 | P0 | 显式反馈 | `rating` 仅允许 1/-1；反馈与用户、会话、消息关联 |
| CHAT-007 | P1 | 问题重写、纠错、混合检索与重排 | A/B 测试集指标不低于基线，且可关闭增强策略 |

### 6.2 M2 校园服务中心

| ID | 优先级 | 需求 | 可验收标准 |
|---|---|---|---|
| SVC-001 | P0 | 办事指南分类、搜索和详情 | 详情包括适用对象、材料、步骤、地点、时间、联系人、更新时间 |
| SVC-002 | P0 | 根据校区/学生类型生成材料清单 | 同一事项对不同适用对象返回可解释的差异项 |
| SVC-003 | P0 | 部门联系人查询 | 支持部门/校区筛选；过期联系人不进入默认结果 |
| WO-001 | P0 | 创建宿舍报修工单 | 必填字段校验；`Idempotency-Key` 重放不产生重复工单 |
| WO-002 | P0 | 工单列表与详情 | 学生仅看本人；处理员只看授权区域；管理员按权限查看 |
| WO-003 | P0 | 状态流转 | `submitted→accepted→processing→completed` 或 `cancelled/rejected`；非法跳转返回 409 |
| WO-004 | P0 | 维修结果评价 | 仅已完成工单、创建者可评价一次 |
| SVC-004 | P1 | 外部办事系统进度查询适配器 | Mock 与真实适配器实现相同接口；外部失败返回可诊断错误 |
| SVC-005 | P1 | 智能填单辅助 | 用户确认后才生成/提交表单；敏感字段不进入模型提示词日志 |

### 6.3 M3 校园社区与互助

| ID | 优先级 | 需求 | 可验收标准 |
|---|---|---|---|
| COM-001 | P0 | 话题、帖子、评论的创建和列表 | 分页稳定；已删除内容不出现在普通用户列表 |
| COM-002 | P0 | 点赞、收藏、举报 | 点赞幂等；举报需原因；同一用户重复操作不重复计数 |
| COM-003 | P0 | 匿名树洞（受控发布） | 前台不返回真实身份；后台反查需特殊权限并产生审计记录 |
| EVT-001 | P0 | 活动发布、筛选、报名、取消 | 容量并发校验；重复报名幂等；截止后不可报名 |
| LOST-001 | P0 | 失物/拾物发布与筛选 | 必填时间、地点、类别、描述；联系方式默认隐藏 |
| LOST-002 | P0 | 候选匹配 | 返回匹配分与原因；候选仅对记录所有者可见 |
| LOST-003 | P0 | 认领与完成 | 关键特征验证通过后才能交换联系信息；双方可确认完成 |
| COM-004 | P1 | 热榜与基础推荐 | 算法参数可配置，能排除违规/已删除/过期内容 |
| MARKET-001 | P1 | 二手物品发布与检索 | 不含在线支付；支持状态、价格、成色、图片与下架 |

### 6.4 M4 平台治理与基础能力

| ID | 优先级 | 需求 | 可验收标准 |
|---|---|---|---|
| IAM-001 | P0 | 账号密码登录、刷新、退出 | 密码哈希存储；失败次数限制；刷新令牌可撤销 |
| IAM-002 | P0 | RBAC 与资源级授权 | 角色权限矩阵可配置；跨模块统一返回 401/403 |
| IAM-003 | P0 | 用户与角色管理 | 禁用用户后已有会话在规定时间内失效 |
| SAFE-001 | P0 | 自定义敏感词和内容检测 | 输入、输出、社区内容分别记录命中策略和处置结果 |
| SAFE-002 | P0 | 举报/审核状态机 | `pending→approved/rejected/escalated`；处置需理由 |
| AUD-001 | P0 | 审计日志 | 登录、权限、配置、审核、知识发布等关键操作不可由普通管理员修改 |
| CFG-001 | P0 | 模型、检索、上传与跨域配置 | 密钥不回显；配置变更校验后生效并记录版本 |
| OPS-001 | P0 | 基础运营看板 | 展示用户、问答、工单、社区、失物和模型调用基础指标 |
| SAFE-003 | P1 | 风险分级与人工升级 | 高风险内容禁止自动公开，并通知授权处理角色 |
| EVAL-001 | P1 | 标准问题集与回归评估 | 知识或提示词变更后可运行并保存结果对比 |

## 7. 数据需求与所有权

### 7.1 数据域所有权

| 数据域 | 唯一写入模块 | 其他模块访问方式 |
|---|---|---|
| 知识库、文档、Chunk、索引任务 | M1 | 内部检索 API / 只读事件，不直接写表 |
| 会话、消息、引用、反馈 | M1 | 脱敏统计 API |
| 指南、联系人、工单、工单事件 | M2 | 公开查询 API / 领域事件 |
| 帖子、评论、活动、报名、失物、匹配 | M3 | 审核 API / 领域事件 |
| 用户、角色、权限、配置、审核、审计 | M4 | 认证上下文 / 授权 API |

### 7.2 数据规则

- 所有主键采用不可枚举的 UUID；对外不得暴露数据库自增规律。
- 所有时间以 UTC 存储，以 ISO 8601 返回，前端按 Asia/Singapore 展示。
- 所有业务表包含 `created_at/updated_at`；需要逻辑删除的表增加 `deleted_at`。
- 个人敏感信息按最小必要原则收集；日志禁止记录密码、令牌、完整身份证号、完整联系方式和心理倾诉原文。
- 匿名是对其他用户匿名，不是无审计责任；身份映射单独保护并设置高权限访问。
- 文档、图片等对象存储只保存对象键，下载使用短期签名地址或受权代理接口。
- 首期关系数据库采用 PostgreSQL；向量数据采用 Chroma（本地开发）并通过仓储接口隔离，便于后续迁移。

## 8. API 契约需求（联调基线）

### 8.1 契约优先

1. 在编写前后端业务实现前冻结 `openapi.yaml`；后端实现、前端 TypeScript 客户端、Mock Server 和契约测试均由它驱动。
2. API 前缀统一为 `/api/v1`；内部模块接口使用 `/internal/v1`，不得从浏览器直接访问。
3. FastAPI 的 `/openapi.json` 必须与仓库内契约一致，CI 检测不兼容变更。
4. 每个接口必须定义：operationId、认证、参数、请求体、成功响应、错误响应、示例和权限要求。
5. 对已发布接口禁止直接删除字段或改变含义；破坏性变更必须升级 API 主版本。

### 8.2 通用请求约定

- 内容类型：普通接口 `application/json; charset=utf-8`；上传使用 `multipart/form-data`；流式聊天使用 `text/event-stream`。
- 认证：`Authorization: Bearer <access_token>`。
- 链路：客户端可传 `X-Request-Id`，后端必须在响应中返回；未传则后端生成。
- 幂等：创建工单、报名、反馈等易重复写接口支持 `Idempotency-Key`。
- 分页：`page` 从 1 开始，`page_size` 默认 20、最大 100；响应返回 `items/page/page_size/total`。
- 排序：只接受接口白名单字段，格式 `sort=-created_at,name`。
- 日期：统一 RFC 3339/ISO 8601；枚举值使用稳定英文小写编码，中文仅用于展示。

### 8.3 通用响应与错误

成功响应：

```json
{
  "code": "OK",
  "message": "success",
  "data": {},
  "request_id": "01J...",
  "timestamp": "2026-07-12T06:00:00Z"
}
```

错误响应：

```json
{
  "code": "WORK_ORDER_INVALID_TRANSITION",
  "message": "当前状态不能执行该操作",
  "details": [{"field": "status", "reason": "completed_to_processing"}],
  "request_id": "01J...",
  "timestamp": "2026-07-12T06:00:00Z"
}
```

HTTP 状态语义：400 参数语义错误；401 未认证；403 无权限；404 资源不存在；409 状态/幂等冲突；413 文件过大；415 格式不支持；422 字段校验失败；429 限流；500 系统错误；502/504 外部依赖失败或超时。

### 8.4 SSE 事件协议

聊天流必须按以下顺序发送，前端不得通过解析自然语言猜测状态：

| event | data 必需字段 | 用途 |
|---|---|---|
| `meta` | `conversation_id,message_id,request_id` | 建立消息关联 |
| `delta` | `sequence,content` | 文本增量，可出现多次 |
| `sources` | `citations[]` | 引用列表，含文档和定位信息 |
| `done` | `finish_reason,usage` | 正常完成 |
| `error` | `code,message,retryable` | 异常终止 |

SSE 断线后，客户端通过消息详情接口查询最终状态；服务端不得在同一消息上重复扣费或重复持久化。

### 8.5 MVP 接口目录

| 模块 | 资源与接口组 |
|---|---|
| M1 | `/knowledge-bases`、`/documents`、`/ingestion-jobs`、`/conversations`、`/chat/completions`、`/chat/stream`、`/chat/feedback` |
| M2 | `/service-guides`、`/departments`、`/work-orders`、`/work-orders/{id}/transitions`、`/work-orders/{id}/rating` |
| M3 | `/topics`、`/posts`、`/comments`、`/events`、`/events/{id}/registrations`、`/lost-found`、`/lost-found/{id}/matches` |
| M4 | `/auth/login`、`/auth/refresh`、`/users`、`/roles`、`/permissions`、`/moderation`、`/configs`、`/audit-logs`、`/dashboard/metrics` |

> 本阶段冻结接口规则和目录；逐字段请求/响应、状态码矩阵、Schema、示例与 Mock 数据将在《详细设计说明书》配套的 OpenAPI 3.1 契约中给出并作为联调依据。

### 8.6 本地联调约定

- 前端开发地址：`http://localhost:5173`。
- 后端开发地址：`http://localhost:8000`，API 为 `http://localhost:8000/api/v1`。
- Swagger UI：`http://localhost:8000/docs`；OpenAPI：`http://localhost:8000/openapi.json`。
- 开发环境仅放行明确的前端 Origin，不使用带凭据的 `*` CORS。
- 前端只通过生成的 `src/api/generated` 客户端访问后端，禁止页面内散落手写 URL。
- 每个模块提供稳定种子数据和 Mock 场景：成功、无权限、空列表、校验失败、冲突、外部超时。

## 9. 非功能性需求

### 9.1 性能与容量

- 非 LLM 普通查询 API 在验收数据量下 P95 < 500 ms。
- 列表均分页；数据库查询不得出现明显 N+1；常用过滤字段建立索引。
- 大文件解析、向量生成、批量导出通过异步任务执行，HTTP 请求只返回任务 ID。
- Chat SSE 首包目标 < 2 秒；完整生成时间单独计量，不混入普通 API 指标。
- 参考目标：1 GB PDF ETL 在 1 小时内完成，首期允许以缩小数据集验证并记录外推结果。

### 9.2 可用性与可恢复性

- 外部模型或校园系统失败时返回明确可重试状态，不让整个站点不可用。
- 异步任务支持幂等重试和失败原因查看。
- 数据库每日备份策略、恢复演练步骤进入部署文档；MVP 至少验证一次本地备份恢复。

### 9.3 安全与隐私

- 密钥仅通过环境变量或密钥服务注入，不写入仓库、日志和前端包。
- 密码使用成熟的自适应哈希算法；JWT 短时 access token + 可撤销 refresh token。
- 入口执行大小限制、文件类型嗅探、速率限制、参数校验和输出编码。
- 文件上传需防路径穿越，文件名不作为存储路径；可疑文件拒绝或隔离。
- 代码执行类 Agent 必须在无宿主文件权限、无默认网络、有限 CPU/内存/时长的沙箱中运行。
- 心理、宿舍、门禁、消费、匿名身份映射等数据采用更严格权限、脱敏展示和更短留存。
- 危机提示必须展示人工渠道和紧急联系方式；具体联系人、通知条件、值班机制需学校确认后才能启用。

### 9.4 可维护性与质量

- Python 代码启用类型标注；前端开启 TypeScript 严格模式。
- 后端核心业务测试覆盖率不低于 70%；每个 P0 API 至少包含成功、认证、权限、校验和冲突测试。
- 使用结构化 JSON 日志，至少包含 request_id、user_id（可脱敏）、module、operation、duration_ms、status。
- 所有合并通过 Pull Request 和至少 1 人 Code Review；数据库迁移和 OpenAPI 变更必须单独标注。
- 新增功能同时更新 OpenAPI、迁移、测试、README 和变更记录，否则不满足完成定义。

### 9.5 可访问性与体验

- 桌面端优先并适配移动浏览器；核心流程无需横向滚动。
- 表单提供明确标签、校验提示与错误恢复；不能只用颜色表达状态。
- AI 生成内容明确标识；引用、风险提示和人工入口必须可见。
- Markdown 渲染默认禁用危险 HTML，外链使用安全属性，防止 XSS。

## 10. 推荐技术基线与环境依赖

### 10.1 选型

| 层 | 选择 | 原因 |
|---|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus | 适合管理台与学生门户共用；组件化、类型友好、开发启动快 |
| 后端 | Python 3.12 + FastAPI + Pydantic v2 | 原生类型校验、OpenAPI 与流式接口支持，便于契约驱动联调 |
| 大语言模型 | DeepSeek V4 Pro（API 模型名 `deepseek-v4-pro`） | 项目固定模型；使用用户提供的 API Key，通过 OpenAI 兼容 Chat Completions 接口调用 |
| 数据访问 | SQLAlchemy 2 + Alembic | 明确 ORM/迁移边界，避免手工同步表结构 |
| 数据库 | PostgreSQL | 关系数据、事务、JSONB 与全文检索能力适合首期统一存储 |
| 缓存/任务 | Redis + Celery（或 Dramatiq，概要设计时二选一） | 会话缓存、限流、异步解析与向量任务 |
| RAG | LangChain/LangGraph + bge-small-zh-v1.5 + Chroma | DeepSeek 负责生成，本地中文 Embedding 负责向量化；与原始总体要求一致 |
| 文件存储 | 本地对象目录（开发）/ MinIO 兼容接口（部署） | 把文件二进制与业务数据库分离 |
| 测试 | pytest + pytest-asyncio + HTTPX；Vitest + Playwright | 覆盖单元、接口、契约和端到端流程 |
| 工程 | Docker Compose、Ruff、mypy、pre-commit、pnpm | 统一四人开发环境和提交质量门禁 |

### 10.2 开发机依赖

- Git、GitHub CLI（可选）、Docker Desktop/Podman、Python 3.12、Node.js 当前 LTS、pnpm。
- 本地模型运行需额外 GPU/内存；首期应支持远程模型 API 和 Mock LLM，不能强制所有成员拥有 GPU。
- `.env.example` 仅列变量名和示例非敏感值：数据库、Redis、对象存储、`DEEPSEEK_BASE_URL=https://api.deepseek.com`、`DEEPSEEK_MODEL=deepseek-v4-pro`、`DEEPSEEK_API_KEY`、JWT 密钥、CORS Origin。真实密钥不得提交到仓库。
- Windows 成员统一通过 Docker Compose 或 WSL2，避免本地 Chroma/编译依赖差异阻塞联调。

## 11. 四人低耦合分工

| 成员 | 主责模块 | 仓库边界 | 对外契约 | 不允许直接修改 |
|---|---|---|---|---|
| A | M1 AI 知识与对话 | `backend/modules/ai_knowledge`、`frontend/modules/chat-kb` | Chat、Knowledge、Ingestion API；发布 `KnowledgePublished` 事件 | M2/M3 业务表、M4 权限表 |
| B | M2 校园服务中心 | `backend/modules/campus_service`、`frontend/modules/services` | Guide、Department、WorkOrder API；消费认证上下文 | M1 会话/索引表、M3 内容表 |
| C | M3 校园社区与互助 | `backend/modules/community`、`frontend/modules/community` | Post、Event、LostFound API；提交审核请求 | M1 索引、M2 工单、M4 审核表 |
| D | M4 平台治理与基础能力 | `backend/modules/platform`、`frontend/modules/admin` | Auth、RBAC、Moderation、Audit、Metrics API | 其他模块领域数据的业务含义 |

协作规则：

1. 采用模块化单体起步，而非四套微服务；每个模块有独立 router/service/repository/schema/migration 分区。
2. 跨模块只能调用公开 service 接口、内部 API 或领域事件，禁止跨模块直接写数据库表。
3. 用户身份由 M4 生成统一认证上下文；业务模块只消费 `user_id/roles/permissions`，不自行实现登录。
4. 审核由 M4 管状态和处置，M3 保留内容事实；审核结果通过接口/事件回写内容可见状态。
5. 每人维护本模块 OpenAPI 标签、Mock 数据、迁移和测试；共享基建变更需小组评审。
6. 每个 Sprint 轮换 1 名集成值班人，负责主干健康、契约差异和端到端冒烟，不替代模块负责人。

## 12. 敏捷开发计划

项目采用 Scrum，开发窗口固定为 10 天，作为一个完整 Sprint 管理。每天进行不超过 15 分钟的 Daily Scrum，第 1 天进行 Sprint Planning，第 10 天进行 Sprint Review 与 Sprint Retrospective。

| Sprint | 目标 | 关键交付 |
|---|---|---|
| 第 1 天 | Sprint Planning 与基线冻结 | 概要设计、原型、模块骨架、OpenAPI 初稿、ERD 初稿、Compose、CI、种子数据 |
| 第 2–3 天 | 四模块最小闭环 | 测试账号登录；文档入库；RAG 单轮问答；指南查询；帖子/失物发布；审计骨架 |
| 第 4–6 天 | 完成 P0 主流程 | 多轮/SSE/引用；报修状态机；评论/活动/匹配；RBAC/审核；模块级测试 |
| 第 7 天 | 全量联调 | 冻结 OpenAPI、生成 TS 客户端、Mock 异常场景、端到端冒烟 |
| 第 8 天 | 测试 | 接口、权限、契约、端到端与基础性能测试 |
| 第 9 天 | 修复与交付整理 | 缺陷修复、README、SQL、测试报告、部署与操作文档 |
| 第 10 天 | Sprint 验收 | Sprint Review、Retrospective、演示和彩排、版本封板 |

### 12.1 Definition of Ready

一个故事进入开发前必须具备：用户价值、业务规则、权限、验收标准、原型或交互说明、OpenAPI 变更、依赖和测试数据。

### 12.2 Definition of Done

完成意味着：实现通过；迁移可执行；OpenAPI 与实现一致；单元/接口测试通过；前端无手写重复模型；异常状态可展示；日志与权限到位；PR 完成评审；README/变更记录已更新。

## 13. 需求追踪矩阵

| 来源 | 原始能力 | 本文落点 | MVP 状态 |
|---|---|---|---|
| 《06》智能生活咨询 | 多轮、场景入口、分类、引用、兜底 | M1、M2 | P0 |
| 《06》事项办理/宿舍 | 指南、材料、进度、报修 | M2 | 指南/报修 P0，其余 P1 |
| 《06》社区/活动/失物 | 帖子、匿名、报名、匹配 | M3 | 核心 P0 |
| 《06》朋辈/二手 | 经验、搭子、交易、租借 | M3 | P1/P2 |
| 《06》心理陪伴 | 倾诉、危机、预约、自助 | 安全专题 | P3，需学校治理确认 |
| 《06》平台/知识/评估/安全 | 看板、内容、反馈、配置、隐私 | M1、M4 | 基础 P0，增强 P1/P2 |
| 《10》知识库与 RAG | 上传、切分、检索、引用、日志 | M1 | P0 |
| 《10》Agent/ETL/微调 | 工具路由、Pandas、双索引、JSONL | M1/研究项 | P1-P3 |
| 《11》文档提交 | PRD、API、SQL、测试、报告 | 全项目 | 全阶段交付 |

## 14. 风险、依赖与已确认决策

### 14.1 主要风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 长期需求过大 | 延期、功能半成品 | 冻结 MVP；未实现功能不进首期导航 |
| 校内系统无接口/无授权 | 进度、水电、门禁无法真实对接 | 适配器 + Mock；接口获批后替换实现 |
| RAG 幻觉或政策过期 | 误导学生 | 只检索已发布内容、强制引用、有效期、低置信兜底 |
| 匿名与心理数据泄露 | 严重隐私/伦理风险 | 最小收集、分级权限、脱敏审计、人工治理 |
| 四人同时改共享代码 | 冲突与联调失败 | 模块边界、契约优先、生成客户端、集成值班 |
| 本地模型环境差异 | 成员无法启动 | Mock LLM + 远程 API + 可替换 Provider |
| Agent 代码执行 | 主机和数据安全风险 | MVP 禁用；后续仅沙箱、无网、限资源执行 |

### 14.2 已确认决策

| 编号 | 决策项 | 确认结论 | 状态 |
|---|---|---|---|
| D1 | 开发周期与 Scrum 节奏 | 10 天，一个 Scrum Sprint，每日站会 | 已确认 |
| D2 | MVP 范围 | 接受本文四模块，不要求 12 大模块全部实现 | 已确认 |
| D3 | 身份认证 | 首期使用内置测试账号、密码登录和 JWT，不对接学校统一认证 | 已确认 |
| D4 | 校内业务系统 | 全部通过 Mock 适配器提供演示数据，前端接口保持可替换 | 已确认 |
| D5 | LLM 与 Embedding | 固定 `deepseek-v4-pro`；用户提供 API Key；Embedding 使用 `bge-small-zh-v1.5` | 已确认 |
| D6 | 部署方式 | Docker Compose 单机部署，用于本地/答辩演示 | 已确认 |
| D7 | 心理危机演示边界 | 不自动通知真实人员，仅展示经过审核的求助渠道与风险提示 | 已确认 |
| D8 | 数据治理演示边界 | 留存期限、匿名反查审批仅做可配置项，不填真实生产策略 | 已确认 |

## 15. 本阶段评审结论

需求基线已确认，可进入概要设计。下一阶段输出系统上下文、模块架构、部署拓扑、数据库域模型、关键时序、异步任务方案和 OpenAPI 设计原则；再下一阶段输出逐接口 Schema、错误码、状态机、表结构、类/包设计和 Vibe Coding 分模块任务包。

## 附录 A：外部技术依据

- FastAPI Features：https://fastapi.tiangolo.com/features/
- Vue + TypeScript：https://vuejs.org/guide/typescript/overview.html
- Vite Guide：https://vite.dev/guide/
- OpenAPI Specification：https://spec.openapis.org/oas/v3.1.1.html
- SQLAlchemy ORM Quick Start：https://docs.sqlalchemy.org/en/20/orm/quickstart.html
- PostgreSQL JSON Types：https://www.postgresql.org/docs/current/datatype-json.html

## 附录 B：后续文档清单

1. 《02-概要设计说明书》
2. 《03-详细设计说明书》
3. `openapi.yaml` 与 Apifox/Swagger 导入说明
4. 《数据库设计说明书》+ ERD + Alembic/SQL
5. 《敏捷产品待办池与 Sprint 计划》
6. 《前后端联调手册》
7. 《测试用例清单》与《测试总结报告》
8. 《部署安装运维手册》
9. 《用户操作手册》
10. 《Vibe Coding 分模块提示词与任务包》
